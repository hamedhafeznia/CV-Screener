# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/build
next-env.d.ts

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# ingest/generation cache — regenerable, never commit
/data/.cache/

# committed on purpose (PRD §10): data/cvs, data/photos,
# data/ground_truth, data/candidates.db, data/index.lance

# local-only scratch scripts (fixtures, one-off probes)
/scripts/_*.ts
